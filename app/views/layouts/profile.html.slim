doctype 5
html
  head
    = render 'layouts/partials/head',
            title: @user.display_name,
            description: @user.bio,
            image: @user.avatar,
            card_type: 'summary',
            card_creator: @user.twitter ? "@#{@user.twitter}" : nil,
            keywords: "gitshowcase, showcase, github, portfolio, #{@user.username}, #{@user.role}"

  body
    = render 'layouts/partials/body'

    .profile-layout
      = render 'components/header-profile', type: 'profile', edit: (user_signed_in? and current_user.id == @user.id)
      = yield
      = render 'components/footer'
