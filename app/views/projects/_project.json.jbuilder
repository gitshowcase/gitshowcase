json.extract! project, :id, :title, :homepage, :repository, :url, :description, :icon, :cover, :language, :hide, :has_manifest, :user_id, :created_at, :updated_at
json.url project_url(project, format: :json)